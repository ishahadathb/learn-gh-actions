name: setup-env
on: push
jobs:
  setup-project:
    runs-on: ubuntu-latest
    steps:
      - use: actions/checkout@v2
      - use: actions/setup-node@v2
        with: 
          node-version: 16
      - run: npm install